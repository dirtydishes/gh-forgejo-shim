# Rust CLI Fixture Area

Rust CLI tests should create temporary homes, repositories, and environment maps at runtime. Static fixtures that are too large to build inline can live here once later phases begin porting behavior.
