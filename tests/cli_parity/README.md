# CLI Parity Fixtures

This directory is reserved for cross-runtime CLI parity fixtures as the Rust rewrite advances. Phase 02 keeps behavior in the installed Python package and only adds the Rust workspace plus an isolated Rust integration-test harness.
