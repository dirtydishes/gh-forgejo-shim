# Phase 04 Turn Doc: C1: Pass the host identity and routing checkpoint

Beads issue: `gh-forgejo-shim-c04`

Phase doc: `docs/implementation/chatgpt-forgejo-pr-compatibility/04-c1-pass-the-host-identity-and-routing-checkpoint.md`

## Accepted Outcome

The host registry, canonical identity, and provider-first routing pattern can support later read behavior without unsafe GitHub fallthrough.

## Orchestration Brief

```json
{
  "phase_issue_id": "gh-forgejo-shim-c04",
  "risk": "high",
  "execution_profile": "adaptive",
  "strategy": "native-multi-agent",
  "implementation_owner": "none unless the combined review set returns findings; the visible coordinator remains the sole possible repair owner",
  "review_independence": "Two separate read-only contexts review the same exact S2-S3 checkpoint range. The thermonuclear role owns module depth and maintainability. The adversarial role owns host isolation, unsafe fallthrough, compatibility, and failure behavior and must not consult the thermonuclear skill.",
  "delegation_plan": [
    "Launch one read-only thermonuclear reviewer for HostRegistry, routing, repository identity, and URL ownership.",
    "Launch one separate read-only adversarial reviewer for alias isolation, credential/transport boundaries, GitHub fallthrough, malformed configuration, and process compatibility.",
    "Use no subordinate helpers inside either review: the two required roles already split the independent evidence surfaces without duplicate missions."
  ],
  "model_and_effort_rationale": "Use fresh full-context reviewers because this checkpoint is a consequential architecture and safety gate. Keep all writes with the coordinator so both reviewers see one frozen commit.",
  "required_evidence": [
    "Both role callbacks share one review-set ID and pass number",
    "The provider and route matrices pass",
    "Both reviewers report no unresolved blocker or required finding",
    "Formatting, locked Clippy, locked workspace tests, exact-commit CI, push, and parity"
  ],
  "user_constraints": [
    "Use only /home/delta/dev/gh-forgejo-shim as the saved checkout",
    "Do not update or reinstall Dirtyloops",
    "Keep one mutable-checkout owner and integration PR #29",
    "Use one combined repair owner and a shared three-pass limit"
  ]
}
```

At C1 entry, execution readiness returned `ready` with `findings: []`; launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. The branch was clean at `6764a5f2c1318b0d2bf3195804e4a1b0bc3ab2bc`, pushed at `0/0` parity, and `gh-forgejo-shim-c04` was claimed only after both gates passed.

## Adaptations

None yet.

## Discoveries And Decisions

None yet.

## Implementation And Delegation Evidence

- The coordinator owns no planned source mutation at pass `0/3`; C1 starts as a pure review checkpoint.
- Two role-specific read-only reviewers will inspect the same frozen S2-S3 range. Their results will be combined before any repair decision.

## Changed Behavior And Files

None yet.

## Review

Review set: `c1-host-routing`

Pass `0/3` reviewed `a2071c81f1215f1f4bae2add01c71eadd653876d..b35482ef278094e0b0c4022c1f5659ea5fe5850a` in two separate read-only contexts.

- Thermonuclear role: `changes_requested`. It found that identity normalization and GitHub classification have inverted ownership between `config` and `provider`, `HostRegistry` discards its validated transport index, dispatcher config reads two snapshots, and `RouteDecision` exposes optional fields that permit mismatched Forgejo states.
- Adversarial role: `changes_requested`. It proved that option payload URLs can override repository identity, configured `GH_HOST` can fall through without a repository, URL user-info can bypass the GitHub exclusion, and uppercase URL schemes can make Forgejo work delegate.

Combined findings remain six distinct failure paths. The coordinator is the sole repair owner for pass `1/3`; both required reviewers must rerun against the repaired exact range.

## CI And Gates

Owner: `phase coordinator`

State: `repair-pass-1-red`

Evidence:

- Red gate at pass `1/3`: `cargo test --locked -p gh-forgejo-shim registry_rejects_transport_user_info` failed because `https://user@github.com/path` registered as a Forgejo transport host.
- Red gate at pass `1/3`: `cargo test --locked -p gh-forgejo-shim parses_url_schemes_case_insensitively` failed because `HTTP://git.example.com/owner/repo.git` parsed `http` as the host.
- Red gate at pass `1/3`: `cargo test --locked -p gh-forgejo-shim option_payload_urls_do_not_override_repository_identity` failed because a GitHub URL in `pr create --body` overrode a Forgejo `GH_REPO`.
- Red gate at pass `1/3`: `cargo test --locked -p gh-forgejo-shim configured_gh_host_stays_local_without_repository_context` failed because an allowlisted `GH_HOST` delegated when no repository was available.
- Provider registry tests: 4 passed.
- Provider-first routing tests: 8 passed, including the full named route matrix.
- ChatGPT build-6720 contract: 5 active passed; 3 later-slice probes remain ignored by design.
- CLI scaffold: 28 passed.
- `cargo fmt --all -- --check`: pass.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: pass.
- `cargo test --workspace --locked`: pass: 135 unit, 5 active ChatGPT contract, 28 CLI scaffold, and 6 HTTP normalization tests.

## PR And Commits

- Integration PR: `https://github.com/dirtydishes/gh-forgejo-shim/pull/29`.
- Checkpoint review range starts at S2 base `a2071c81f1215f1f4bae2add01c71eadd653876d` and will end at the pushed orchestration-record commit.

## Beads Updates And Follow-Ups

- Claimed `gh-forgejo-shim-c04` only after its exact readiness and launch gates passed.

## Plan Amendments

The user selected the adaptive profile when creating the loop. This supersedes the plan's earlier orchestrator-callback profile clause without changing product intent or phase boundaries.

## Context To Keep

Read the accepted plan, this phase doc, and live Beads state before work.

## Closeout

Active at combined repair pass `1/3`. Four observable safety failures now have red tests. One cohesive production repair must also resolve the two structural ownership findings before both review roles rerun.
