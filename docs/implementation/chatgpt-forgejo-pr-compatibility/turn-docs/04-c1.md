# Phase 04 Turn Doc: C1: Pass the host identity and routing checkpoint

Beads issue: `gh-forgejo-shim-c04`

Phase doc: `docs/implementation/chatgpt-forgejo-pr-compatibility/04-c1-pass-the-host-identity-and-routing-checkpoint.md`

## Accepted Outcome

The host registry, canonical identity, and provider-first routing pattern can support later read behavior without unsafe GitHub fallthrough.

## Orchestration Brief

```json
{
  "phase_issue_id": "gh-forgejo-shim-c04",
  "risk": "high",
  "execution_profile": "adaptive",
  "strategy": "native-multi-agent",
  "implementation_owner": "none unless the combined review set returns findings; the visible coordinator remains the sole possible repair owner",
  "review_independence": "Two separate read-only contexts review the same exact S2-S3 checkpoint range. The thermonuclear role owns module depth and maintainability. The adversarial role owns host isolation, unsafe fallthrough, compatibility, and failure behavior and must not consult the thermonuclear skill.",
  "delegation_plan": [
    "Launch one read-only thermonuclear reviewer for HostRegistry, routing, repository identity, and URL ownership.",
    "Launch one separate read-only adversarial reviewer for alias isolation, credential/transport boundaries, GitHub fallthrough, malformed configuration, and process compatibility.",
    "Use no subordinate helpers inside either review: the two required roles already split the independent evidence surfaces without duplicate missions."
  ],
  "model_and_effort_rationale": "Use fresh full-context reviewers because this checkpoint is a consequential architecture and safety gate. Keep all writes with the coordinator so both reviewers see one frozen commit.",
  "required_evidence": [
    "Both role callbacks share one review-set ID and pass number",
    "The provider and route matrices pass",
    "Both reviewers report no unresolved blocker or required finding",
    "Formatting, locked Clippy, locked workspace tests, exact-commit CI, push, and parity"
  ],
  "user_constraints": [
    "Use only /home/delta/dev/gh-forgejo-shim as the saved checkout",
    "Do not update or reinstall Dirtyloops",
    "Keep one mutable-checkout owner and integration PR #29",
    "Use one combined repair owner and the user-authorized final pass-5 limit"
  ]
}
```

At C1 entry, execution readiness returned `ready` with `findings: []`; launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. The branch was clean at `6764a5f2c1318b0d2bf3195804e4a1b0bc3ab2bc`, pushed at `0/0` parity, and `gh-forgejo-shim-c04` was claimed only after both gates passed.

At pass-4 resumption, the explicit Beads amendment changed the canonical leaf projection. The installed `prepareLaunchableLeaf` helper refreshed only that attestation to `8d4a2aa3adb6ee95b1125798ec5be6b9bcb77387a8ba383373dfbe95fb15be3f`. The exact execution-readiness command then returned `ready` with `findings: []`; launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. C1 was reclaimed only after both gates passed at pushed head `88862284c167f5c4a13abe2ec0fdce5d521f5c81` with a clean checkout and `0/0` parity.

## Adaptations

- Pass `2/3` adds `tests/cli_scaffold.rs` to the C1 repair evidence surface. This narrow adjustment provides process-level proof for the exact routing escapes without changing the accepted outcome, dependencies, rollback, one-PR policy, or repair limit.
- On 2026-08-25, the user authorized one exceptional pass `4/4` after both reviewers blocked pass `3/3`. This amendment changes only C1's shared repair limit. Pass 4 must replace the split routing and local-handler grammar with one command-aware interpretation, fail closed on unsupported or ambiguous option shapes, and prove successful local execution against the intended repository. No fifth pass is authorized.
- On 2026-08-25, the user authorized one final pass `5/5` after the adversarial reviewer retained two findings at pass `4/4`. This amendment changes only C1's shared repair limit. Pass 5 owns documented `repo view -R/--repo` support and authoritative failure for malformed explicit repository selectors; all other accepted scope, behavior, review, CI, and rollback rules remain unchanged.

## Discoveries And Decisions

- Canonical transport identity belongs to `provider`, not `config`. `config` keeps compatibility re-exports while the registry validates and owns normalized transport keys.
- A Forgejo route now carries one `ForgejoTarget`. Its profile and canonical host cannot be absent or disagree, while its repository remains optional for host-only `auth` and `api` commands.
- Repository URLs are identity inputs only in the positional target slot of commands that accept them. URLs inside option payloads are ordinary data.
- Repository identity is now selected once by `CommandProviderTarget`; local handlers consume the routed repository and no longer reinterpret raw repository flags or positional targets.

## Implementation And Delegation Evidence

- The coordinator owns no planned source mutation at pass `0/3`; C1 starts as a pure review checkpoint.
- Two role-specific read-only reviewers will inspect the same frozen S2-S3 range. Their results will be combined before any repair decision.
- Red evidence was frozen and pushed at `55e4d0062e183c82bcbe834d41b19543f8082363` before production work.
- The cohesive repair was frozen and pushed at `341592f009717b9d67e7c2303be0f9cac20e89b5`; branch parity was `0/0` after both pushes.
- Pass-2 red evidence was frozen and pushed at `28bfa167eac3914d073687e038e025c58a6e6215`.
- The pass-2 source repair was frozen and pushed at `b34c5bb1f37b66e100586c3d1a858379e5f46a0f`; branch parity was `0/0`.
- Final pass-3 red evidence was frozen and pushed at `4ef065a6110bb079863d76c84f679ddb2ecb0bf0`.
- The final pass-3 source repair was frozen and pushed at `c581a7c4c38e5708c8542f34dc863afcfa49dd82`; branch parity was `0/0`.
- Pass `4/4` uses direct coordinator mutation because the adaptive profile permits it, the coordinator already owns the only mutable checkout, and the source repair spans one coupled routing and local-handler grammar seam. One bounded read-only grammar audit may inspect installed `gh`; both required final reviewers remain separate contexts.
- The bounded installed-`gh` audit confirmed Debian `gh 2.46.0` pflag behavior: boolean shorts cluster, a value-bearing short ends a cluster and consumes its suffix or next token, and attached value forms are valid. It made no changes.
- Pass-4 red process evidence was frozen and pushed at `30f06ea188aae97dc0a8e3c6a4380aab56d8368f` before production work.
- The pass-4 design correction was frozen and pushed at `8e5ec4f8bfd8c4acccfdebd37d0f6cf9b8c3f3a5`; the checkout was clean and branch parity was `0/0` before both required reviews started.
- Pass `5/5` remains direct coordinator work because the adaptive profile permits it, the coordinator owns the sole mutable checkout, and the two accepted findings share the existing invocation and routing seam. No implementation helper is useful for this narrow, fully reproduced repair; both required reviewers will remain separate read-only contexts.

## Changed Behavior And Files

- `src/provider.rs`: owns checked host normalization and GitHub classification, rejects user-info, validates credential hosts and API-root authority, and retains the transport-to-profile index.
- `src/config.rs`: produces `Config` and `HostRegistry` from one parsed runtime snapshot.
- `src/repo.rs`: limits URL detection to command-specific positional targets and treats supported URL schemes case-insensitively.
- `src/routing.rs`: represents delegation and Forgejo routing as distinct enum variants, routes configured `GH_HOST` without repository context, and passes a coherent target to local execution.
- `src/read_only.rs` and `src/trace.rs`: consume the typed route accessors instead of rebuilding optional route state.
- `src/repo.rs`: owns the command-aware repository and hostname target grammar, including explicit flags, accepted option values, and positional URL or repository slots.
- `src/read_only.rs`: consumes the already routed repository; uppercase HTTP(S) targets retain issue and pull numbers.
- `src/config.rs` and `src/provider.rs`: expose and propagate fallible canonical host normalization at config ingestion.
- `tests/cli_scaffold.rs`: proves provider isolation across nine bidirectional managed-`gh` processes and malformed-config fail-closed behavior.
- `src/repo.rs`: accepts Cobra-compatible attached short values, recognizes the installed `gh` flags that affect target scanning, and continues through conservative option/value pairs in unsupported command trees to find explicit repository selectors.
- `tests/cli_scaffold.rs`: expands the provider-isolation matrix to 28 bidirectional processes, including attached short values, omitted accepted flags, and explicit selectors before and after unsupported-command value options.
- `src/invocation.rs`: owns one parsed command interpretation for command identity, option arity, attached values, short clusters, provider selectors, inherited help, and ambiguous grammar.
- `src/routing.rs` and `src/read_only.rs`: consume the same `ParsedInvocation`; ambiguous grammar takes a local `Reject` route and native handlers receive only the parser's canonical argument form.
- `tests/provider_routing.rs`: owns the focused provider matrix and proves that attached `-R`, clustered `-cR`, and clustered `-dR` forms exit successfully after requesting the selected repository. The general CLI scaffold is below 1,000 lines again.

## Review

Review set: `c1-host-routing`

Pass `0/3` reviewed `a2071c81f1215f1f4bae2add01c71eadd653876d..b35482ef278094e0b0c4022c1f5659ea5fe5850a` in two separate read-only contexts.

- Thermonuclear role: `changes_requested`. It found that identity normalization and GitHub classification have inverted ownership between `config` and `provider`, `HostRegistry` discards its validated transport index, dispatcher config reads two snapshots, and `RouteDecision` exposes optional fields that permit mismatched Forgejo states.
- Adversarial role: `changes_requested`. It proved that option payload URLs can override repository identity, configured `GH_HOST` can fall through without a repository, URL user-info can bypass the GitHub exclusion, and uppercase URL schemes can make Forgejo work delegate.

Combined findings remain six distinct failure paths. The coordinator is the sole repair owner for pass `1/3`; both required reviewers must rerun against the repaired exact range.

Pass `1/3` reviewed `a2071c81f1215f1f4bae2add01c71eadd653876d..ed05e8ac1c157ca96498a58e28cf7279deac32d0`. Both roles returned `changes_requested`.

- Thermonuclear role: the partial URL-target scanner duplicates and drifts from accepted command grammar. Valid `-c`, `-i`, `--exclude`, and `-e` forms can still make a Forgejo positional URL delegate.
- Adversarial role: confirmed the grammar drift and added consumed `--repo`/`--hostname` payload escapes, plain `repo view` cross-provider targets, silently discarded malformed canonical config, and uppercase HTTP(S) issue/PR number loss.

The overlap combines into three pass-2 repair surfaces: one shared command-aware provider-target parser, fallible canonical config ingestion, and case-insensitive downstream number parsing. The coordinator remains the sole repair owner for pass `2/3`.

Pass `2/3` reviewed `a2071c81f1215f1f4bae2add01c71eadd653876d..585abdc8afff790af2536fb362f1677671b42353`. The thermonuclear role approved with `findings: []`. The adversarial role returned three findings that combine into one final grammar surface: attached short values, accepted API and PR flags absent from the scanner, and unsupported command value options could all hide a later explicit provider target or lose the explicit target itself.

The coordinator remains the sole repair owner for final pass `3/3`. Both roles must review the same exact repaired range. Any required finding after that review exhausts the shared limit and blocks.

Pass `3/3` reviewed `a2071c81f1215f1f4bae2add01c71eadd653876d..a0749eab819bf6d0652c0046dc624abd6a4b7a63`. Both roles returned `changes_requested` with required findings.

- Adversarial role: valid shorthand clusters ending in inherited `-R`, such as `-cR` and `-dR`, still lose the explicit repository. It also reproduced provider reversals when an unsupported command consumes a selector-shaped option payload.
- Thermonuclear role: confirmed that unknown option arity cannot be inferred safely from token shape, and proved a Forgejo-context `release create --notes --repo=...` command can delegate. It also found that local handlers reject valid attached forms even when routing stays local, while the process table treats any local nonzero exit as success. The enlarged table pushed `tests/cli_scaffold.rs` past the required 1,000-line boundary.

These findings exhaust the shared three-pass repair limit. No fourth repair is permitted under the accepted checkpoint policy.

The user then amended that policy for one exceptional pass `4/4`. The coordinator reopened C1 in Beads before source mutation. The pass-4 repair must correct the design seam rather than extend the example list: one parsed command interpretation owns provider selectors and the arguments local handlers consume; installed `gh` global and accepted command flags include attached and clustered short forms; unknown option arity is never guessed; and an unsupported or ambiguous shape that could hide a selector fails closed. Both required roles rerun on one frozen pass-4 head. Any required finding ends C1 without pass 5.

Pass `4/4` reviewed `a2071c81f1215f1f4bae2add01c71eadd653876d..8e5ec4f8bfd8c4acccfdebd37d0f6cf9b8c3f3a5` in the two required independent contexts. The thermonuclear role approved with `findings: []`. The adversarial role returned two required findings:

- `repo view -R`, which the project documents and the local handler accepts, is absent from `ParsedInvocation`'s repository-selector grammar and now fails as ambiguous.
- A malformed explicit `--repo` selector is not rejected after repository parsing fails. Detection falls through to `GH_REPO`, which can delegate across providers.

The exceptional four-pass limit was exhausted. The user then amended the shared limit to `5/5` for one final narrow repair. C1 must remain unclaimed until the amended Beads projection receives a fresh canonical attestation and both exact readiness gates pass. Any required finding after pass 5 exhausts the new limit and blocks.

## CI And Gates

Owner: `phase coordinator`

State: `pass-5-amendment-recorded-admission-pending`

Evidence:

- Red gate at pass `2/3`: `consumed_route_flag_values_do_not_override_provider_identity` failed because a `--repo=github.com` body value overrode Forgejo `GH_REPO`.
- Red gate at pass `2/3`: `command_grammar_preserves_explicit_provider_targets` failed because `issue view -c <Forgejo URL>` delegated from GitHub context.
- Red gate at pass `2/3`: `malformed_canonical_hosts_fail_configuration_loading` failed because a user-info host was silently discarded.
- Red gate at pass `2/3`: `uppercase_http_targets_preserve_pull_and_issue_numbers` failed because uppercase HTTPS PR view produced an empty object instead of pull 7.
- All four pass-2 red probes pass after the repair.
- Command target grammar table: 51 accepted short, long, attached, flag, and value shapes pass.
- Managed-`gh` provider isolation table: 9 bidirectional processes pass; malformed canonical config also fails closed without invoking real `gh`.
- Final pass-3 red gate: the installed-flag grammar unit table failed first at attached `-q.number`, returning no explicit Forgejo issue target.
- Final pass-3 red gate: the process isolation table delegated a Forgejo issue URL after attached `-q.number` from a GitHub environment.
- The installed-flag and attached-short grammar table now passes, including API, PR, auth, workflow, and release shapes.
- The managed-`gh` provider isolation table now passes 28 bidirectional processes; the earlier consumed-payload cases remain green.
- Red gate at pass `1/3`: `cargo test --locked -p gh-forgejo-shim registry_rejects_transport_user_info` failed because `https://user@github.com/path` registered as a Forgejo transport host.
- Red gate at pass `1/3`: `cargo test --locked -p gh-forgejo-shim parses_url_schemes_case_insensitively` failed because `HTTP://git.example.com/owner/repo.git` parsed `http` as the host.
- Red gate at pass `1/3`: `cargo test --locked -p gh-forgejo-shim option_payload_urls_do_not_override_repository_identity` failed because a GitHub URL in `pr create --body` overrode a Forgejo `GH_REPO`.
- Red gate at pass `1/3`: `cargo test --locked -p gh-forgejo-shim configured_gh_host_stays_local_without_repository_context` failed because an allowlisted `GH_HOST` delegated when no repository was available.
- The same four focused probes pass after the repair.
- Provider registry tests: 5 passed.
- Provider-first routing tests: 10 passed, including the full named route matrix.
- ChatGPT build-6720 contract: 5 active passed; 3 later-slice probes remain ignored by design.
- CLI scaffold: 28 passed.
- `cargo fmt --all -- --check`: pass.
- `cargo clippy --workspace --all-targets --locked -- -D warnings`: pass.
- `cargo test --workspace --locked`: pass: 145 unit, 5 active ChatGPT contract, 30 CLI scaffold, and 6 HTTP normalization tests.
- Final independent review: failed. Both roles returned required findings at exact head `a0749eab819bf6d0652c0046dc624abd6a4b7a63`.
- CI admission: not started. A green local suite cannot override a failed required review, and any CI result would not satisfy C1 acceptance.
- Pass-4 CI remains pending until the design correction passes focused and full local gates and both required reviewers approve the same frozen head.
- Pass-4 focused provider tests pass: 4 tests, including exact selected-repository request paths and fail-closed ambiguity.
- Pass-4 full local gates pass: formatting, locked Clippy with warnings denied, and locked workspace tests (148 unit; 5 active ChatGPT with 3 planned ignored; 29 CLI scaffold; 6 HTTP normalization; 4 provider routing).
- Exact-commit CI is green at `8e5ec4f8bfd8c4acccfdebd37d0f6cf9b8c3f3a5`: pull-request run `32824978173` and push run `32824973094` both passed `rust` and `release package smoke`.
- Final independent review failed despite green local and CI gates: thermonuclear approved, but adversarial returned the two required findings listed above. The required review gate controls C1 acceptance.

## PR And Commits

- Integration PR: `https://github.com/dirtydishes/gh-forgejo-shim/pull/29`.
- Checkpoint review range starts at S2 base `a2071c81f1215f1f4bae2add01c71eadd653876d` and will end at the pushed orchestration-record commit.
- Red evidence: `55e4d0062e183c82bcbe834d41b19543f8082363`.
- Repair source: `341592f009717b9d67e7c2303be0f9cac20e89b5`.
- Pass-2 red evidence: `28bfa167eac3914d073687e038e025c58a6e6215`.
- Pass-2 repair source: `b34c5bb1f37b66e100586c3d1a858379e5f46a0f`.
- Final pass-3 red evidence: `4ef065a6110bb079863d76c84f679ddb2ecb0bf0`.
- Final pass-3 repair source: `c581a7c4c38e5708c8542f34dc863afcfa49dd82`.
- Final frozen review head: `a0749eab819bf6d0652c0046dc624abd6a4b7a63`.
- Pass-4 red process evidence: `30f06ea188aae97dc0a8e3c6a4380aab56d8368f`.
- Pass-4 frozen repair and review head: `8e5ec4f8bfd8c4acccfdebd37d0f6cf9b8c3f3a5`.

## Beads Updates And Follow-Ups

- Claimed `gh-forgejo-shim-c04` only after its exact readiness and launch gates passed.
- Appended the pass-1 red, repair, local-gate, push, and parity evidence before updating this mirror.
- Appended the pass-2 review, red, repair, process-table, local-gate, push, and parity evidence before updating this mirror.
- Appended the pass-3 review, red, repair, installed-flag, process-table, local-gate, push, and parity evidence before updating this mirror.
- Marked `gh-forgejo-shim-c04` blocked after both final review callbacks confirmed required findings at pass `3/3`; pushed the Beads state to its Dolt remote before updating this mirror.
- Reopened `gh-forgejo-shim-c04` as `in_progress` and recorded the one-time `repair_pass_limit: 4` amendment in Beads before changing this mirror or any production source.
- Recorded the pass-4 red, repair, local-gate, exact-commit CI, and both review verdicts in Beads; set C1 to `blocked`; and pushed the Dolt state before updating this mirror.
- Recorded the user-authorized final `repair_pass_limit: 5` amendment and its exact two-finding scope in Beads, kept C1 blocked pending fresh admission, and pushed the Dolt state before updating this mirror.

## Plan Amendments

The user selected the adaptive profile when creating the loop. This supersedes the plan's earlier orchestrator-callback profile clause without changing product intent or phase boundaries.

On 2026-08-25, the user authorized one exceptional C1 repair pass and raised only this checkpoint's shared limit from three to four. The accepted product outcome, dependencies, file and interface intent, TDD order, reviewer roles, CI gates, rollback rules, and one-PR policy remain unchanged.

Later on 2026-08-25, the user authorized one final C1 repair pass and raised only this checkpoint's shared limit from four to five. Pass 5 owns the documented `repo view -R/--repo` route and malformed explicit-selector failure authority. The accepted outcome, constraints, dependencies, acceptance criteria, TDD order, reviewer roles, CI gates, rollback rules, and one-PR policy remain unchanged.

## Context To Keep

Read the accepted plan, this phase doc, and live Beads state before work.

## Closeout

Final combined repair pass `5/5` is authorized but not yet admitted. Beads and both resume documents record the amendment while C1 remains blocked. Refresh only the canonical launch attestation, require execution readiness `ready` with `findings: []` and launch readiness `launchable` with `findings: []`, then reclaim C1 before tests or production source change.
