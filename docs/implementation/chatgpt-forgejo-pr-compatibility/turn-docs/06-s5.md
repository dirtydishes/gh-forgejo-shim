# Phase 06 Turn Doc: S5: Enforce the command deadline and truthful tracing

Beads issue: `gh-forgejo-shim-c06`

Phase doc: `docs/implementation/chatgpt-forgejo-pr-compatibility/06-s5-enforce-the-command-deadline-and-truthful-tracing.md`

## Accepted Outcome

A user gets a bounded result or clear timeout, and the trace states which output it observed.

## Orchestration Brief

```json
{
  "phase_issue_id": "gh-forgejo-shim-c06",
  "risk": "high",
  "execution_profile": "adaptive",
  "strategy": "direct-coordinator-tdd",
  "implementation_owner": "the visible coordinator remains the sole mutable-checkout owner",
  "review_independence": "One fresh read-only reviewer inspects the frozen S5 range for one shared monotonic deadline, streaming and ordering regressions, truthful byte-count states, and secret redaction. The coordinator owns any combined repair pass within the shared three-pass limit.",
  "delegation_plan": [
    "Keep the coupled deadline, process I/O, and trace implementation in the one coordinator-owned checkout.",
    "After one exact S5 commit is frozen and pushed, launch one independent read-only deadline-and-observability review against that SHA."
  ],
  "model_and_effort_rationale": "The phase crosses request timing, child-process output, and trace semantics. Direct TDD keeps one clock and output model coherent; a fresh review context tests failure paths without adding a second writer.",
  "required_evidence": [
    "One monotonic five-second budget is shared across all requests and enrichment for a command",
    "Observed noninteractive commands preserve output order and record exact stdout and stderr byte counts",
    "Inherited or interactive streaming stays uncaptured and records byte counts as unknown",
    "Timeout, exit, duration, provider, command class, and redaction have literal schema assertions",
    "Formatting, locked Clippy, locked workspace tests, independent review, exact-commit CI, push, and parity"
  ],
  "user_constraints": [
    "Use only /home/delta/dev/gh-forgejo-shim as the mutable checkout",
    "Do not update or reinstall Dirtyloops",
    "Keep integration PR #29 as the one external PR",
    "Stop if the deadline resets, output order changes, interactive behavior regresses, or traces claim false precision"
  ]
}
```

At S5 entry, exact execution readiness returned `ready` with `findings: []`; live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. The saved checkout and origin branch matched `e70717faef0202e766e84dc4e9eca6adf3245cb4` at `0/0` parity. `gh-forgejo-shim-c06` was claimed only after both gates passed, and the claimed Dolt state was pushed.

## Adaptations

- Add `crates/gh-forgejo-shim/src/read_only.rs` to the narrow S5 ownership surface. `routing.rs` owns the command lifetime and `forgejo.rs` owns per-request timeout use, but `read_only.rs` constructs the client between them. Passing one shared `CommandDeadline` through that caller is the smallest change that prevents the five-second budget from resetting. This does not change the accepted outcome, dependencies, constraints, acceptance criteria, review roles, repair limit, CI gates, rollback, or one-PR rule.
- Add `crates/gh-forgejo-shim/src/deadline.rs` and its `src/lib.rs` export so `CommandDeadline` stays a small shared interface instead of expanding the 975-line Forgejo transport module. Add `crates/gh-forgejo-shim/tests/cli_scaffold.rs` to freeze the literal trace schema at the real managed-`gh` process seam. This is a file-scope adjustment only. It preserves the outcome, dependencies, constraints, acceptance criteria, review role, shared three-pass repair limit, CI gates, rollback, one-PR rule, and sole mutable-checkout owner.

## Discoveries And Decisions

- The first independent review approved the shared deadline, timeout mapping, counting writers, provider and command-class fields, observed-versus-unknown schema, redaction, and ordinary untraced streaming.
- One required defect remains: the initial capture predicate matched whole command classes. With tracing enabled, forms such as `pr checks --watch` and inherited help could run through buffered `Command::output()` instead of inherited streaming.
- Shared repair pass `1/3` narrows capture to the five exact noninteractive build-6720 command shapes. Other forms keep inherited output and report byte counts as unknown.

## Implementation And Delegation Evidence

- Direct coordinator work is justified because deadline creation, request consumption, child-process capture, and trace truthfulness form one coupled state path. No mutable helper is needed.
- TDD order: freeze the shared-deadline and literal trace-state failures before production mutation; preserve inherited streaming controls throughout.

## Changed Behavior And Files

- Source implementation through frozen review head `190bb4f0acd48aa2e92b0a3bfa8c1cda5cd46f81` creates one command deadline, spends its remaining budget across Forgejo requests, maps expiry to `Forgejo command deadline exceeded`, and records literal trace output-observation states.
- The required pass-1 repair remains test-first and limited to the trace capture policy plus process evidence for inherited watch and help output.

## Review

Review set: `s5-deadline-observability`

- Pass `0/3` inspected exact range `f0d09bdf9e48c24a33529f91ad795ecdf2fa9672..190bb4f0acd48aa2e92b0a3bfa8c1cda5cd46f81` on a clean, pushed head.
- Verdict: `changes_requested` with one high required finding at `crates/gh-forgejo-shim/src/trace.rs:62`.
- Finding: broad command-class capture can buffer inherited output for `pr checks --watch`, help, and other non-contract forms. Repair pass `1/3` must recognize only the five frozen build-6720 probes and prove inherited streaming before child exit.

## CI And Gates

Owner: `phase coordinator`

State: `claimed-tdd-pending`

Evidence:

- Exact S5 execution readiness: `ready`, `findings: []`.
- Live launch readiness: `boundary_status: ready`, `status: launchable`, `findings: []`.
- Admission identity: `e70717faef0202e766e84dc4e9eca6adf3245cb4`, clean, pushed, `0/0`.
- Amended projection readiness: `ready`, `findings: []`, Beads hash `5bd86070b0462f08f4876ba29b951715f4043a8eda3c9e39a3b10450440e607b`.
- Amended live launch: `boundary_status: ready`, `status: launchable`, `findings: []`; reclaim followed the gate.
- The deadline-module and process-test adaptation refreshed only S5's canonical execution attestation to Beads projection `3507aa60b352ea8ad54679c59938774ad9c14e2ea10dbc12d57b645c06e733f5`. Exact execution readiness returned `ready` with `findings: []`; live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []` before reclaim.
- Frozen pre-review head `190bb4f0acd48aa2e92b0a3bfa8c1cda5cd46f81` passed the full locked local gate.
- Exact-commit pull-request run `32840663644` passed `rust` job `97779202482` and `release package smoke` job `97779202167`.
- Exact-commit push run `32840659593` passed `rust` job `97779189345` and `release package smoke` job `97779188986`.

## PR And Commits

- Integration PR: `https://github.com/dirtydishes/gh-forgejo-shim/pull/29`.
- Frozen review head: `190bb4f0acd48aa2e92b0a3bfa8c1cda5cd46f81`.

## Beads Updates And Follow-Ups

- Claimed `gh-forgejo-shim-c06` only after both exact readiness gates passed and pushed the claim to the Dolt remote.
- Recorded the narrow `read_only.rs` ownership adaptation, refreshed only S5's canonical execution attestation, reopened for live-ready validation, and reclaimed only after both gates passed.
- Recorded the pass-0 review finding and opened shared repair pass `1/3` before production mutation.

## Plan Amendments

The user selected the adaptive profile when creating the loop. This supersedes the plan's earlier orchestrator-callback profile clause without changing product intent or phase boundaries.

## Context To Keep

Read the accepted plan, this phase doc, and live Beads state before work.

## Closeout

S5 repair pass `1/3` is active. The next mutation freezes inherited-streaming and inherited-help process repros before narrowing the capture predicate.
