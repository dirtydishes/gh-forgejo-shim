# Phase 08 Turn Doc: C2: Pass the PR discovery checkpoint

Beads issue: `gh-forgejo-shim-c08`

Phase doc: `docs/implementation/chatgpt-forgejo-pr-compatibility/08-c2-pass-the-pr-discovery-checkpoint.md`

## Accepted Outcome

The pull request module, test seam, shared deadline, and pagination rules are sound before view projections grow around them.

## Orchestration Brief

```json
{
  "phase_issue_id": "gh-forgejo-shim-c08",
  "risk": "high",
  "execution_profile": "adaptive",
  "strategy": "frozen-head-dual-independent-review",
  "implementation_owner": "the visible coordinator remains the sole mutable-checkout owner and combines any required findings into at most three TDD repair passes",
  "review_roles": [
    "One fresh thermonuclear reviewer inspects module depth, ownership, seams, condition growth, file size, and test design.",
    "A separate fresh adversarial reviewer challenges credentials, timeouts, pagination, partial results, empty-result truthfulness, and GitHub delegation."
  ],
  "frozen_review_range": "fa4d1f5fca25ddb864928b08f05350a7f3b3a763..<C2 admission head>",
  "required_evidence": [
    "Both reviewers inspect the same clean pushed SHA and report no unresolved required finding",
    "Exact auth isolation, shared-deadline, build-6720 process, pagination, provider-routing, and workspace gates pass",
    "Exact pull-request and push CI pass on the frozen checkpoint SHA",
    "Any repair freezes red evidence first and stays within the shared three-pass limit"
  ],
  "user_constraints": [
    "Use only /home/delta/dev/gh-forgejo-shim as the mutable checkout",
    "Do not update or reinstall Dirtyloops",
    "Keep PR #29 as the one external integration PR",
    "Reviewers remain read-only and never repair their own findings"
  ]
}
```

At C2 entry, exact execution readiness returned `ready` with `findings: []`; live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. The saved checkout, origin branch, and PR #29 matched `b9da700917aa1135998383d3aa767cefbd12a307` at clean `0/0` parity. `gh-forgejo-shim-c08` was claimed only after both gates passed and the claimed Dolt state was pushed.

## Adaptations

None yet.

## Discoveries And Decisions

- Review pass `0/3` found four required issues grouped into two repair seams. Pass `1/3` will address the complete combined set; reviewers remain read-only.
- Auth configuration seam: every recognized JSON host field must be authoritative, so empty, invalid, or conflicting fields fail before token use. The text parser and shared host/token record policy must move behind a focused module so `auth.rs` falls from 1,163 lines to below 1,000 and JSON/text rules cannot drift.
- Pull-request discovery seam: successful page responses must be a JSON array containing only objects; wrong shapes must fail before pagination or stdout. The older list path and paginated discovery must also reuse one canonical head predicate.

## Implementation And Delegation Evidence

- Direct source mutation is not justified before review findings exist. The two required reviewers receive the same frozen range and SHA and remain read-only.
- If findings exist, the coordinator groups them by failure path, freezes the smallest red tests, and owns the one mutable checkout. Reviewers do not repair their own findings.
- Pass `1/3` TDD order: freeze conflicting-JSON-host tests and malformed-page process tests; then extract the focused auth-config module, apply one authoritative host-field rule, decode pages strictly, and reuse the pull-request head predicate. Run the full locked gate and send both reviewers the same frozen repair SHA.
- Red commit `a2705ba9b8c8b00e469eaace4550b7bc8fcc0951` is pushed at `0/0` parity before production mutation. Two auth tests return `other-secret` for conflicting, empty, non-string, or matching-key-conflicting host fields. Two process tests return exit `0` for `{}` and mixed object/string PR pages instead of failing with empty stdout.

## Changed Behavior And Files

- Added private `src/auth_config.rs` and moved credential-file parsing plus its focused tests out of `auth.rs`. JSON and text records now share one rule: a token is eligible only when every stated host field is a valid match; a matching JSON map key may supply the host only when no stated field conflicts.
- `auth.rs` is now 883 lines, below the required 1,000-line boundary.
- Paginated PR discovery now validates the raw success body as an array containing only objects. A non-array or mixed page returns `Forgejo API returned an invalid pull request page` before pagination or stdout.
- The legacy and paginated pull-list paths now call the same head predicate. `forgejo.rs` is 975 lines.

## Review

Review set `c2-pr-discovery`, pass `0/3`, inspected exact range `fa4d1f5fca25ddb864928b08f05350a7f3b3a763..b8071ef0a9a8319955addbfbae0b3da0e7b00cee`.

- Thermonuclear verdict: `changes_requested`. Required findings: `auth.rs` crossed 1,000 lines while adding a second structured-config grammar, and head matching is duplicated between `forgejo.rs` and `pull_requests/list.rs`.
- Adversarial verdict: `changes_requested`. Required findings: conflicting recognized JSON host fields can authorize the wrong token, and malformed PR page JSON can be shortened into a false terminal empty page.
- Both reviewers verified the exact clean head and `0/0` parity. The combined repair limit remains `3`; pass `1/3` owns all four findings.

## CI And Gates

Owner: `phase coordinator`

State: `repair-pass-1-review-pending`

Evidence:

- Exact C2 execution readiness: `ready`, `findings: []`.
- Live launch readiness: `boundary_status: ready`, `status: launchable`, `findings: []`.
- Admission identity: `b9da700917aa1135998383d3aa767cefbd12a307`, clean, pushed, `0/0`, PR #29 exact.
- Frozen checkpoint head `b8071ef0a9a8319955addbfbae0b3da0e7b00cee`: 21 auth tests, 3 shared-deadline/trace tests, 7 pagination tests, 7 active build-6720 tests, and the full 226-test locked workspace gate passed; two later probes remain ignored.
- Exact pull-request run `32847105931` passed `rust` job `97799247887` and `release package smoke` job `97799247761`.
- Exact push run `32847101166` passed `rust` job `97799232268` and `release package smoke` job `97799232144`.
- Repair pass-1 focused gates passed: 10 auth-config tests, 14 auth integration tests, 7 pagination tests, and all 9 active build-6720 tests; two later probes remain ignored.
- Repair pass-1 full locked gate passed at `302bbeae7b379c7282e2cc842d534ed97d4a1be1`: formatting, all-target Clippy with warnings denied, and 231 active workspace tests; two later probes remain ignored.

## PR And Commits

- Integration PR: `https://github.com/dirtydishes/gh-forgejo-shim/pull/29`.
- Review pass-0 head: `b8071ef0a9a8319955addbfbae0b3da0e7b00cee`.
- Repair pass-1 red head: `a2705ba9b8c8b00e469eaace4550b7bc8fcc0951`.
- Repair pass-1 production head: `302bbeae7b379c7282e2cc842d534ed97d4a1be1`.

## Beads Updates And Follow-Ups

- Claimed `gh-forgejo-shim-c08` only after both exact readiness gates passed and pushed the claim to the Dolt remote.
- Recorded both pass-0 review verdicts, the combined two-seam repair scope, and pass `1/3` in canonical Beads state; Dolt is pushed.
- Recorded the exact pass-1 production head, file-size evidence, and full locked local gate in canonical Beads state; Dolt is pushed before review.

## Plan Amendments

The user selected the adaptive profile when creating the loop. This supersedes the plan's earlier orchestrator-callback profile clause without changing product intent or phase boundaries.

## Context To Keep

Read the accepted plan, this phase doc, and live Beads state before work.

## Closeout

C2 repair pass `1/3` is locally green and pushed. Both required reviewers and exact-commit PR/push CI remain before checkpoint acceptance.
