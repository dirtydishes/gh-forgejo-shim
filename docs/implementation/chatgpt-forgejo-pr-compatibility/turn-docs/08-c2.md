# Phase 08 Turn Doc: C2: Pass the PR discovery checkpoint

Beads issue: `gh-forgejo-shim-c08`

Phase doc: `docs/implementation/chatgpt-forgejo-pr-compatibility/08-c2-pass-the-pr-discovery-checkpoint.md`

## Accepted Outcome

The pull request module, test seam, shared deadline, and pagination rules are sound before view projections grow around them.

## Orchestration Brief

```json
{
  "phase_issue_id": "gh-forgejo-shim-c08",
  "risk": "high",
  "execution_profile": "adaptive",
  "strategy": "frozen-head-dual-independent-review",
  "implementation_owner": "the visible coordinator remains the sole mutable-checkout owner and combines any required findings into at most three TDD repair passes",
  "review_roles": [
    "One fresh thermonuclear reviewer inspects module depth, ownership, seams, condition growth, file size, and test design.",
    "A separate fresh adversarial reviewer challenges credentials, timeouts, pagination, partial results, empty-result truthfulness, and GitHub delegation."
  ],
  "frozen_review_range": "fa4d1f5fca25ddb864928b08f05350a7f3b3a763..<C2 admission head>",
  "required_evidence": [
    "Both reviewers inspect the same clean pushed SHA and report no unresolved required finding",
    "Exact auth isolation, shared-deadline, build-6720 process, pagination, provider-routing, and workspace gates pass",
    "Exact pull-request and push CI pass on the frozen checkpoint SHA",
    "Any repair freezes red evidence first and stays within the shared three-pass limit"
  ],
  "user_constraints": [
    "Use only /home/delta/dev/gh-forgejo-shim as the mutable checkout",
    "Do not update or reinstall Dirtyloops",
    "Keep PR #29 as the one external integration PR",
    "Reviewers remain read-only and never repair their own findings"
  ]
}
```

At C2 entry, exact execution readiness returned `ready` with `findings: []`; live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. The saved checkout, origin branch, and PR #29 matched `b9da700917aa1135998383d3aa767cefbd12a307` at clean `0/0` parity. `gh-forgejo-shim-c08` was claimed only after both gates passed and the claimed Dolt state was pushed.

## Adaptations

None yet.

## Discoveries And Decisions

None yet.

## Implementation And Delegation Evidence

- Direct source mutation is not justified before review findings exist. The two required reviewers receive the same frozen range and SHA and remain read-only.
- If findings exist, the coordinator groups them by failure path, freezes the smallest red tests, and owns the one mutable checkout. Reviewers do not repair their own findings.

## Changed Behavior And Files

None yet.

## Review

Not started.

## CI And Gates

Owner: `phase coordinator`

State: `claimed-freeze-pending`

Evidence:

- Exact C2 execution readiness: `ready`, `findings: []`.
- Live launch readiness: `boundary_status: ready`, `status: launchable`, `findings: []`.
- Admission identity: `b9da700917aa1135998383d3aa767cefbd12a307`, clean, pushed, `0/0`, PR #29 exact.

## PR And Commits

- Integration PR: `https://github.com/dirtydishes/gh-forgejo-shim/pull/29`.

## Beads Updates And Follow-Ups

- Claimed `gh-forgejo-shim-c08` only after both exact readiness gates passed and pushed the claim to the Dolt remote.

## Plan Amendments

The user selected the adaptive profile when creating the loop. This supersedes the plan's earlier orchestrator-callback profile clause without changing product intent or phase boundaries.

## Context To Keep

Read the accepted plan, this phase doc, and live Beads state before work.

## Closeout

C2 is admitted and claimed. The next commit freezes the exact two-reviewer and CI checkpoint head.
