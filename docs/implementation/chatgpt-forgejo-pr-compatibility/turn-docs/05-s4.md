# Phase 05 Turn Doc: S4: Make authentication use canonical host identity

Beads issue: `gh-forgejo-shim-c05`

Phase doc: `docs/implementation/chatgpt-forgejo-pr-compatibility/05-s4-make-authentication-use-canonical-host-identity.md`

## Accepted Outcome

`gh auth status --active --hostname ALIAS` finds the canonical host credential without exposing the token.

## Orchestration Brief

```json
{
  "phase_issue_id": "gh-forgejo-shim-c05",
  "risk": "high",
  "execution_profile": "adaptive",
  "strategy": "direct-coordinator-tdd",
  "implementation_owner": "the visible coordinator remains the sole mutable-checkout owner",
  "review_independence": "One fresh read-only reviewer inspects the frozen S4 range for credential-host isolation, file permissions, precedence, and token leakage. The coordinator owns any combined repair pass within the shared three-pass limit.",
  "delegation_plan": [
    "Keep implementation and all repository writes with the visible coordinator.",
    "After one exact S4 commit is frozen and pushed, launch one independent read-only credential-boundary review against that SHA."
  ],
  "model_and_effort_rationale": "The code change is narrow, but authentication is security-sensitive. Direct TDD keeps one writer; a fresh review context supplies independent failure-path scrutiny.",
  "required_evidence": [
    "Process-level alias and canonical auth-status commands use credential_host and do not print tokens",
    "An unrelated host cannot read the credential",
    "Linux fj, shim storage, environment, and existing macOS discovery keep their precedence",
    "Formatting, locked Clippy, locked workspace tests, independent review, exact-commit CI, push, and parity"
  ],
  "user_constraints": [
    "Use only /home/delta/dev/gh-forgejo-shim as the mutable checkout",
    "Do not update or reinstall Dirtyloops",
    "Keep integration PR #29 as the one external PR",
    "Stop on cross-host credential use, token disclosure, or credential copying"
  ]
}
```

At S4 entry, exact execution readiness returned `ready` with `findings: []`; live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. The saved checkout, origin branch, and PR #29 matched `fa4d1f5fca25ddb864928b08f05350a7f3b3a763` at `0/0` parity. `gh-forgejo-shim-c05` was claimed only after both gates passed, and the claimed Dolt state was pushed.

The caller-surface adaptation changed the canonical Beads projection. The installed `prepareLaunchableLeaf` helper refreshed only S4's ownership metadata and execution-readiness attestation to Beads projection hash `228075dee047f7b2dea690b46403b46c0d7c68254e408b2dc926d59842d83592`. The first live check correctly reported that an already claimed issue was absent from `bd ready`. S4 was reopened without source mutation; exact execution readiness then returned `ready` with `findings: []`, and live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. S4 was reclaimed only after both gates passed, and the Dolt state was pushed.

## Adaptations

- Add `crates/gh-forgejo-shim/src/read_only.rs` to the narrow S4 ownership surface. `auth.rs` can discover a token for a host, but the routed `HostProfile` owns the distinct `credential_host`; the native dispatcher must pass that field into discovery. This is the smallest file-scope change that can satisfy the accepted outcome. It does not change provider routing, repository identity, accepted behavior, dependencies, review roles, repair limit, CI gates, rollback, or the one-PR rule.

## Discoveries And Decisions

- Canonical and transport host identity remain display and API-routing inputs. `credential_host` is the only key for token lookup once a configured Forgejo profile has resolved.
- S4 will add the Linux `fj` keys path without copying or migrating any token.

## Implementation And Delegation Evidence

- Direct coordinator work is justified because the implementation is one coupled credential lookup seam across `auth.rs`, its caller in `read_only.rs`, and auth-specific contract tests.
- TDD order: freeze Linux-path, credential-host, unrelated-host, precedence, and no-leak failures before changing production code.

## Changed Behavior And Files

- Planned: `crates/gh-forgejo-shim/src/auth.rs` adds Linux `fj` discovery.
- Planned: `crates/gh-forgejo-shim/src/read_only.rs` uses the resolved profile's `credential_host` for discovery while retaining the canonical host in output and API identity.
- Planned: `crates/gh-forgejo-shim/tests/chatgpt_build_6720.rs` proves the product-level alias command contract.

## Review

Not started.

## CI And Gates

Owner: `phase coordinator`

State: `admitted` — red evidence pending

Evidence:

- Exact S4 execution readiness: `ready`, `findings: []`.
- Live launch readiness: `boundary_status: ready`, `status: launchable`, `findings: []`.
- Admission identity: `fa4d1f5fca25ddb864928b08f05350a7f3b3a763`, PR #29 exact, clean, pushed, `0/0`.
- Amended projection readiness: `ready`, `findings: []`, Beads hash `228075dee047f7b2dea690b46403b46c0d7c68254e408b2dc926d59842d83592`.
- Amended live launch: `boundary_status: ready`, `status: launchable`, `findings: []`; reclaim followed the gate.

## PR And Commits

- Integration PR: `https://github.com/dirtydishes/gh-forgejo-shim/pull/29`.

## Beads Updates And Follow-Ups

- Claimed `gh-forgejo-shim-c05` only after both exact readiness gates passed and pushed the claim to the Dolt remote.
- Record the narrow `read_only.rs` ownership adaptation and refresh only S4's execution-readiness attestation before production mutation.

## Plan Amendments

The user selected the adaptive profile when creating the loop. This supersedes the plan's earlier orchestrator-callback profile clause without changing product intent or phase boundaries.

## Context To Keep

Read the accepted plan, this phase doc, and live Beads state before work.

## Closeout

S4 is claimed after the original and amended readiness sequences both passed. The next mutation is test-only red evidence for the accepted credential-host boundary.
