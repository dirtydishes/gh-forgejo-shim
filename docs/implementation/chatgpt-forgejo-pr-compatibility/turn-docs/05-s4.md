# Phase 05 Turn Doc: S4: Make authentication use canonical host identity

Beads issue: `gh-forgejo-shim-c05`

Phase doc: `docs/implementation/chatgpt-forgejo-pr-compatibility/05-s4-make-authentication-use-canonical-host-identity.md`

## Accepted Outcome

`gh auth status --active --hostname ALIAS` finds the canonical host credential without exposing the token.

## Orchestration Brief

```json
{
  "phase_issue_id": "gh-forgejo-shim-c05",
  "risk": "high",
  "execution_profile": "adaptive",
  "strategy": "direct-coordinator-tdd",
  "implementation_owner": "the visible coordinator remains the sole mutable-checkout owner",
  "review_independence": "One fresh read-only reviewer inspects the frozen S4 range for credential-host isolation, file permissions, precedence, and token leakage. The coordinator owns any combined repair pass within the shared three-pass limit.",
  "delegation_plan": [
    "Keep implementation and all repository writes with the visible coordinator.",
    "After one exact S4 commit is frozen and pushed, launch one independent read-only credential-boundary review against that SHA."
  ],
  "model_and_effort_rationale": "The code change is narrow, but authentication is security-sensitive. Direct TDD keeps one writer; a fresh review context supplies independent failure-path scrutiny.",
  "required_evidence": [
    "Process-level alias and canonical auth-status commands use credential_host and do not print tokens",
    "An unrelated host cannot read the credential",
    "Linux fj, shim storage, environment, and existing macOS discovery keep their precedence",
    "Formatting, locked Clippy, locked workspace tests, independent review, exact-commit CI, push, and parity"
  ],
  "user_constraints": [
    "Use only /home/delta/dev/gh-forgejo-shim as the mutable checkout",
    "Do not update or reinstall Dirtyloops",
    "Keep integration PR #29 as the one external PR",
    "Stop on cross-host credential use, token disclosure, or credential copying"
  ]
}
```

At S4 entry, exact execution readiness returned `ready` with `findings: []`; live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. The saved checkout, origin branch, and PR #29 matched `fa4d1f5fca25ddb864928b08f05350a7f3b3a763` at `0/0` parity. `gh-forgejo-shim-c05` was claimed only after both gates passed, and the claimed Dolt state was pushed.

The caller-surface adaptation changed the canonical Beads projection. The installed `prepareLaunchableLeaf` helper refreshed only S4's ownership metadata and execution-readiness attestation to Beads projection hash `228075dee047f7b2dea690b46403b46c0d7c68254e408b2dc926d59842d83592`. The first live check correctly reported that an already claimed issue was absent from `bd ready`. S4 was reopened without source mutation; exact execution readiness then returned `ready` with `findings: []`, and live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. S4 was reclaimed only after both gates passed, and the Dolt state was pushed.

## Adaptations

- Add `crates/gh-forgejo-shim/src/read_only.rs` to the narrow S4 ownership surface. `auth.rs` can discover a token for a host, but the routed `HostProfile` owns the distinct `credential_host`; the native dispatcher must pass that field into discovery. This is the smallest file-scope change that can satisfy the accepted outcome. It does not change provider routing, repository identity, accepted behavior, dependencies, review roles, repair limit, CI gates, rollback, or the one-PR rule.

## Discoveries And Decisions

- Canonical and transport host identity remain display and API-routing inputs. `credential_host` is the only key for token lookup once a configured Forgejo profile has resolved.
- S4 will add the Linux `fj` keys path without copying or migrating any token.

## Implementation And Delegation Evidence

- Direct coordinator work is justified because the implementation is one coupled credential lookup seam across `auth.rs`, its caller in `read_only.rs`, and auth-specific contract tests.
- TDD order: freeze Linux-path, credential-host, unrelated-host, precedence, and no-leak failures before changing production code.
- Test-only commit `d7911944b8980626cfd87524ccf119e10439d25e` is pushed at `0/0` parity. The Linux-path unit probe and alias auth-status process probe fail for the expected missing behavior; the stored-auth precedence control passes.
- Repair pass `1/3` red commit `afc5a6a8043b98c73270d7cd8db61cddca95d685` is pushed at `0/0` parity. The two tests reproduce cross-entry and hostname-substring token reads before the repair.
- Repair pass `2/3` red commit `b740a1fa9248aae1e606d59fc2e0136d239f664a` is pushed at `0/0` parity. The exact reviewer YAML returns the later entry's token when that token appears before its host field.
- Repair pass `2/3` head `83ad87d7eb97d48af4054ab8a7bce05472c7fc02` is pushed at `0/0` parity. Text discovery now evaluates complete YAML list entries, requires every recognized host field in the candidate entry to match the requested normalized host, and then reads a token from that same entry regardless of key order.
- Final repair pass `3/3` red commit `4b4529b964e4b24970d67c69d40072ced3804360` is pushed at `0/0` parity. The exact reviewer repros return `nested-secret` from a child sequence and `other-secret` while ignoring a quoted conflicting host field. The same frozen tests also cover a later top-level token and an empty host followed by a valid URL.
- Final repair pass `3/3` head `2a12a64dc0c631f4a8bfc8245ff784279de56af6` is pushed at `0/0` parity and exact on PR #29. Indentation scope now bounds each list item; only direct fields count; exactly one valid normalized host selector is required; malformed, duplicate, or conflicting host occurrences fail closed.

## Changed Behavior And Files

- Planned: `crates/gh-forgejo-shim/src/auth.rs` adds Linux `fj` discovery.
- Planned: `crates/gh-forgejo-shim/src/read_only.rs` uses the resolved profile's `credential_host` for discovery while retaining the canonical host in output and API identity.
- Planned: `crates/gh-forgejo-shim/tests/chatgpt_build_6720.rs` proves the product-level alias command contract.
- Implemented: `crates/gh-forgejo-shim/src/auth.rs` adds Linux `fj` discovery and binds text-config credentials to one direct host entry.
- Implemented: `crates/gh-forgejo-shim/src/read_only.rs` uses the resolved profile's `credential_host` for discovery while retaining canonical display and API identity.
- Implemented: `crates/gh-forgejo-shim/tests/chatgpt_build_6720.rs` proves the alias command contract without printing the token.

## Review

Review set: `s4-auth-credential-boundary`

Pass `0/3` reviewed `fa4d1f5fca25ddb864928b08f05350a7f3b3a763..bfa6a2c51d4afb68cd135acafc44aaa63f644c3f` in one fresh read-only context. The reviewer returned `changes_requested` with one required finding: text/YAML discovery finds the requested host as a substring, then scans the next 2,000 characters for any token line. A target entry without a token can therefore read a later unrelated host's token, and a hostname containing the requested hostname can do the same. Repair pass `1/3` owns exact normalized host-entry binding and the two negative repro classes.

Pass `1/3` reviewed `fa4d1f5fca25ddb864928b08f05350a7f3b3a763..1cf4412d1e06f3cfa3d7616ef0c47bf71eeb40cd` in a new read-only context. The reviewer returned `changes_requested` with one required finding: YAML mapping key order is not fixed, so a later list item can put its token before its host field and inherit the prior entry's active match. Repair pass `2/3` owns whole-entry token and exact-host binding independent of key order.

Pass `2/3` reviewed `fa4d1f5fca25ddb864928b08f05350a7f3b3a763..83ad87d7eb97d48af4054ab8a7bce05472c7fc02` in a fresh read-only context. The reviewer returned `changes_requested` with two required findings at one text-entry boundary: nested child mappings and later out-of-scope mappings can still contribute a token; quoted, duplicate, conflicting, or malformed host-field occurrences can be discarded instead of failing closed. Final repair pass `3/3` owns indentation-aware direct fields and authoritative validation of every recognized host-field occurrence.

Pass `3/3` reviewed `fa4d1f5fca25ddb864928b08f05350a7f3b3a763..2a12a64dc0c631f4a8bfc8245ff784279de56af6` in a fresh read-only context. The reviewer approved with `findings: []`. Direct indentation, nested items, outdents, quoted and malformed keys, duplicate or conflicting selectors, key order, normal YAML, Linux discovery, precedence, permissions, alias identity, and output/trace redaction all passed.

## CI And Gates

Owner: `phase coordinator`

State: `accepted`

Evidence:

- Exact S4 execution readiness: `ready`, `findings: []`.
- Live launch readiness: `boundary_status: ready`, `status: launchable`, `findings: []`.
- Admission identity: `fa4d1f5fca25ddb864928b08f05350a7f3b3a763`, PR #29 exact, clean, pushed, `0/0`.
- Amended projection readiness: `ready`, `findings: []`, Beads hash `228075dee047f7b2dea690b46403b46c0d7c68254e408b2dc926d59842d83592`.
- Amended live launch: `boundary_status: ready`, `status: launchable`, `findings: []`; reclaim followed the gate.
- Red unit gate: `discovers_linux_fj_keys_json_only_for_matching_credential_host` failed with `None` instead of `linux-secret`.
- Red process gate: `forgejo_alias_auth_status_uses_credential_host_without_leaking_tokens` failed because alias auth status reported the canonical host as not logged in.
- Precedence control: `stored_shim_auth_precedes_linux_fj_keys_json` passed.
- Repair-head local gate: formatting, locked Clippy, and 209 active workspace tests passed; 3 future probes remained ignored.
- Exact-commit CI at `bfa6a2c51d4afb68cd135acafc44aaa63f644c3f`: pull-request run `32835303024` and push run `32835296306` both passed `rust` and `release package smoke`.
- Independent pass `0/3`: `changes_requested` for the cross-host YAML/text lookup described above. This required review gate overrides the green CI result.
- Repair pass-1 red gate: lookup for a target entry without a token returned `other-secret` from the following host entry.
- Repair pass-1 red gate: lookup for `auth.target.test` returned `substring-secret` from `auth.target.test.evil`.
- Repair pass-1 full locked gate: formatting, Clippy with warnings denied, and 211 active tests passed; 3 future probes remained ignored.
- Repair pass-1 exact CI: pull-request run `32836031686` and push run `32836027555` passed both named jobs on `1cf4412d1e06f3cfa3d7616ef0c47bf71eeb40cd`.
- Independent pass `1/3`: `changes_requested` because a later YAML item can put `token` before `host` and inherit the prior entry match.
- Repair pass-2 red gate: the exact token-before-host YAML returned `other-secret` for `auth.target.test`.
- Repair pass-2 full locked gate: formatting, Clippy with warnings denied, and 213 active tests passed; 3 future probes remained ignored.
- Repair pass-2 positive control: a matching list entry with `token` before `host` returns its own token.
- Repair pass-2 exact CI at `83ad87d7eb97d48af4054ab8a7bce05472c7fc02`: pull-request run `32836935366` and push run `32836931015` passed both `rust` and `release package smoke`.
- Independent pass `2/3`: `changes_requested` because nested or later mappings can contribute a token and because ignored quoted, duplicate, or malformed host fields can let another selector win.
- Final pass-3 red gate: the direct-entry scope test failed with `nested-secret`; the every-host-occurrence test failed with `other-secret`. Both focused commands exited 101 before production mutation.
- Final pass-3 full locked gate: formatting, Clippy with warnings denied, and 215 active tests passed; 3 future probes remained ignored.
- Final independent pass `3/3`: `approve`, `findings: []` on `2a12a64dc0c631f4a8bfc8245ff784279de56af6`.
- Final exact CI: pull-request run `32838058693` and push run `32838053551` passed `rust` and `release package smoke` on that SHA.

## PR And Commits

- Integration PR: `https://github.com/dirtydishes/gh-forgejo-shim/pull/29`.
- S4 red evidence: `d7911944b8980626cfd87524ccf119e10439d25e`.
- S4 first repair head: `bfa6a2c51d4afb68cd135acafc44aaa63f644c3f`.
- S4 repair pass-1 red evidence: `afc5a6a8043b98c73270d7cd8db61cddca95d685`.
- S4 repair pass-1 head: `1cf4412d1e06f3cfa3d7616ef0c47bf71eeb40cd`.
- S4 repair pass-2 red evidence: `b740a1fa9248aae1e606d59fc2e0136d239f664a`.
- S4 repair pass-2 head: `83ad87d7eb97d48af4054ab8a7bce05472c7fc02`.
- S4 repair pass-3 red evidence: `4b4529b964e4b24970d67c69d40072ced3804360`.
- S4 final repair head: `2a12a64dc0c631f4a8bfc8245ff784279de56af6`.

## Beads Updates And Follow-Ups

- Claimed `gh-forgejo-shim-c05` only after both exact readiness gates passed and pushed the claim to the Dolt remote.
- Record the narrow `read_only.rs` ownership adaptation and refresh only S4's execution-readiness attestation before production mutation.
- Recorded the exact S4 red commit, two expected failures, and passing precedence control in Beads; pushed the Dolt state before production mutation.
- Recorded the pass-0 review finding, pass `1/3` repair admission, and exact repair-head CI in Beads; pushed the Dolt state before the next red test.
- Recorded the exact pass-1 red commit and both observed cross-host token values in Beads; pushed the Dolt state before production mutation.
- Recorded the pass-1 review finding, pass `2/3` repair admission, and exact pass-1 CI in Beads; pushed the Dolt state before the next red test.
- Recorded the exact pass-2 red commit and observed cross-entry token in Beads; pushed the Dolt state before production mutation.
- Recorded the pass-2 review findings, final pass `3/3` admission, and exact pass-2 CI in Beads; pushed the Dolt state before the final red tests.
- Recorded the exact pass-3 red commit and both observed leaked token values in Beads; pushed the Dolt state before production mutation.
- Recorded the final repair, approval, local gate, and exact CI in Beads; closed `gh-forgejo-shim-c05` and pushed the Dolt state.

## Plan Amendments

The user selected the adaptive profile when creating the loop. This supersedes the plan's earlier orchestrator-callback profile clause without changing product intent or phase boundaries.

## Context To Keep

Read the accepted plan, this phase doc, and live Beads state before work.

## Closeout

S4 is accepted and closed. Alias auth uses `credential_host` while canonical host identity remains visible, unrelated text-config entries cannot supply a token, all local and exact CI gates pass, and the final independent review reports no findings.
