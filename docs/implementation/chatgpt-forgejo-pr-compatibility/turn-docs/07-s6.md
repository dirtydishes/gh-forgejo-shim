# Phase 07 Turn Doc: S6: Make PR discovery honor `@me` and pagination

Beads issue: `gh-forgejo-shim-c07`

Phase doc: `docs/implementation/chatgpt-forgejo-pr-compatibility/07-s6-make-pr-discovery-honor-me-and-pagination.md`

## Accepted Outcome

ChatGPT can truthfully decide whether the current branch has a pull request.

## Orchestration Brief

```json
{
  "phase_issue_id": "gh-forgejo-shim-c07",
  "risk": "high",
  "execution_profile": "adaptive",
  "strategy": "direct-coordinator-tdd",
  "implementation_owner": "the visible coordinator remains the sole mutable-checkout owner",
  "review_independence": "One fresh read-only reviewer inspects the frozen S6 range for identity filtering, pagination termination, caps, timeout truthfulness, and output compatibility. The coordinator owns any combined repair pass within the shared three-pass limit.",
  "delegation_plan": [
    "Keep the coupled PullRequestSource interface, pagination loop, normalization, and process contract in the one coordinator-owned checkout.",
    "After one exact S6 head is frozen and pushed, launch one independent read-only review against that SHA."
  ],
  "model_and_effort_rationale": "The phase joins API pagination, stable user identity, command defaults, and a fixed command deadline. Direct TDD keeps one termination and filtering model coherent; the independent review probes adversarial page and identity cases without adding a second writer.",
  "required_evidence": [
    "Resolve @me through GET /user and compare stable login values, never display names",
    "Paginate 50 entries per request through page-2 matches and terminal empty pages",
    "Honor state=all, head, author, default 30, explicit limits, and the 1,000-candidate safety cap without partial success",
    "Activate the S6 build-6720 process probe and preserve exact JSON output shape",
    "Formatting, locked Clippy, locked workspace tests, independent review, exact-commit pull-request and push CI, push, and parity"
  ],
  "user_constraints": [
    "Use only /home/delta/dev/gh-forgejo-shim as the mutable checkout",
    "Do not update or reinstall Dirtyloops",
    "Keep integration PR #29 as the one external PR",
    "Stop on partial timeout or cap results, display-name comparison, ignored --author, or an unresolved review or CI gate"
  ]
}
```

At S6 entry, exact execution readiness returned `ready` with `findings: []`; live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. The saved checkout, origin branch, and PR #29 matched `50f62381fb1fa9a9a6d8d739eb56b95512346f65` at `0/0` parity. `gh-forgejo-shim-c07` was claimed only after both gates passed, and the claimed Dolt state was pushed.

## Adaptations

- Added `crates/gh-forgejo-shim/tests/chatgpt_build_6720.rs` to S6's exact ownership list. The phase already names activation of the frozen build-6720 `@me` probe as required evidence. Pagination and cap unit tests remain in the owned `src/pull_requests/list.rs`. This narrow bookkeeping change preserves the accepted outcome, dependencies, constraints, review roles, three-pass repair limit, CI gates, rollback rules, and one-PR policy.
- Reopened the claimed leaf only to refresh its canonical execution-readiness attestation after that ownership correction. Exact execution readiness returned `ready` with `findings: []`; live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. The same coordinator reclaimed S6 only after both gates passed and pushed the Dolt state.
- Added `crates/gh-forgejo-shim/tests/provider_routing.rs` after the first full locked gate found one stale request assertion. The command still routed to `selected/repo`, but the test expected `state=open` without S6's required `limit=50&page=1`. This adjustment owns only that expected process request and preserves every accepted product, review, repair, CI, rollback, and one-PR rule.
- The refreshed execution gate returned `ready` with `findings: []`; live launch readiness returned `boundary_status: ready`, `status: launchable`, and `findings: []`. S6 was reclaimed only after both gates passed and Dolt was pushed. The expected request now includes `state=open&limit=50&page=1`; the focused routing test and full locked suite pass.

## Discoveries And Decisions

None yet.

## Implementation And Delegation Evidence

- Direct coordinator work is justified because the source adapter, pagination loop, limit and cap policy, output projection, and command deadline form one coupled behavior path. No mutable helper is used.
- TDD order: activate the frozen S6 build-6720 probe and freeze page/identity/limit/cap failures before production mutation. A fresh read-only reviewer remains independent after the exact head is frozen.
- Red process commit `466275419cd75d0ac232740d64a870ff6b19784a` activates `forgejo_pr_list_resolves_author_me` before production changes. The exact test failed with `[]` instead of Alice's matching PR because the old one-request path consumed the `/user` object as a pull list, did not resolve the stable login, and never reached the pulls response. The commit is pushed at `0/0` parity.
- Green identity commit `2aad71532735b973c37e3a91085c7a492218a4d3` establishes the private `PullRequestSource`, resolves `@me` from `login`, filters author and head before output, and makes the process probe pass. It intentionally fetches only page 1 and does not yet implement the default limit or cap.
- Pagination red commit `887ca735548b66a6cef6a4f887a778b049d90ca2` freezes six failures before the loop changes: page-2 discovery, terminal-page detection, default 30, an explicit limit spanning pages, authoritative failure beyond 1,000 candidates, and later-page error propagation without partial data. The stable-login, display-name, and head-label control passes.
- Green pagination commit `acdf0885e72046ed65f4ada3ba4779de5c5c0202` passes all seven private-source tests and the full build-6720 contract file. It requests 50 entries per page, filters before applying the output limit, defaults to 30, stops on the requested count or terminal page, and returns an error without output after a later-page failure or more than 1,000 candidates.
- The redacted live Dirtypages replay used an isolated temporary host profile plus a symlink to the existing owner-only `fj` credential file. The exact loopback-alias command called `https://git.dirtydishes.dev/api/v1`, returned exit `0`, stdout `[]`, and empty stderr. Exact-path cleanup removed the temporary profile and symlink. The absent global host config and unrelated dirty Dirtypages worktree remained unchanged.

## Changed Behavior And Files

- `src/pull_requests/list.rs` owns stable-login resolution, author/head/base/draft/state filters, 50-entry pagination, default and explicit limits, terminal-page detection, and the 1,000-candidate failure boundary behind `PullRequestSource`.
- `src/forgejo.rs` exposes one crate-private page request while retaining the older list method for other command paths; the file remains below 1,000 lines.
- `src/read_only.rs` now parses `--author` and hands the complete list query to the pull-request module before normalization or output.
- `tests/chatgpt_build_6720.rs` activates the S6 `@me` process probe and updates alias fixtures for the required `/user` request.
- `tests/provider_routing.rs` expects S6's deterministic page-1 query without changing its provider or repository assertion.

## Review

Fresh independent review inspected exact range `50f62381fb1fa9a9a6d8d739eb56b95512346f65..1533e138ec3c89b0ba87eb828399f78e547ebfa1` on the clean frozen head. Verdict: `approve`; findings: `[]`.

The reviewer confirmed stable-login-only `@me` matching, all list filters, 50-entry pages, terminal detection, default and explicit limits, authoritative cap and later-page errors, buffered output, shared-deadline propagation, configured API-root query behavior, and the private real/fake source seam. `forgejo.rs` remains 986 lines and `pull_requests/list.rs` remains a cohesive 332-line module.

## CI And Gates

Owner: `phase coordinator`

State: `accepted`

Evidence:

- Exact S6 execution readiness: `ready`, `findings: []`.
- Live launch readiness: `boundary_status: ready`, `status: launchable`, `findings: []`.
- Post-adaptation execution readiness: `ready`, `findings: []`.
- Post-adaptation live launch readiness: `boundary_status: ready`, `status: launchable`, `findings: []`.
- Admission identity: `50f62381fb1fa9a9a6d8d739eb56b95512346f65`, clean, pushed, `0/0`, PR #29 exact.
- First full locked S6 gate: format and all-target Clippy passed; workspace tests stopped at `provider_routing::clustered_pr_flags_run_locally_against_the_selected_repo`, where the actual correct request was `state=open&limit=50&page=1` and the stale expected request lacked pagination.
- Final full locked S6 gate: format passed; all-target locked Clippy passed with warnings denied; the locked workspace passed 163 unit, 7 active build-6720, 29 CLI, 6 HTTP, 18 provider-routing, and 3 trace-observability tests, for 226 active tests. Two future build-6720 probes remain ignored for S7/S8 and S9.
- Exact pull-request CI run `32846527037` passed `rust` job `97797402591` and `release package smoke` job `97797403041` on `1533e138ec3c89b0ba87eb828399f78e547ebfa1`.
- Exact push CI run `32846523034` passed `rust` job `97797390139` and `release package smoke` job `97797390509` on the same SHA.
- Local HEAD, origin, and PR #29 all matched the frozen head at clean `0/0` parity before closeout.

## PR And Commits

- Integration PR: `https://github.com/dirtydishes/gh-forgejo-shim/pull/29`.
- Red process evidence: `466275419cd75d0ac232740d64a870ff6b19784a`.
- Green identity seam: `2aad71532735b973c37e3a91085c7a492218a4d3`.
- Red pagination evidence: `887ca735548b66a6cef6a4f887a778b049d90ca2`.
- Green pagination implementation: `acdf0885e72046ed65f4ada3ba4779de5c5c0202`.
- Frozen review and CI head: `1533e138ec3c89b0ba87eb828399f78e547ebfa1`.

## Beads Updates And Follow-Ups

- Claimed `gh-forgejo-shim-c07` only after both exact readiness gates passed and pushed the claim to the Dolt remote.
- Recorded both narrow ownership adaptations, each refreshed execution and launch gate, both red heads, the green head, local gate, independent review, and exact CI runs in canonical Beads state.
- Closed `gh-forgejo-shim-c07` after all acceptance evidence passed and pushed the Dolt close receipt.

## Plan Amendments

The user selected the adaptive profile when creating the loop. This supersedes the plan's earlier orchestrator-callback profile clause without changing product intent or phase boundaries.

## Context To Keep

Read the accepted plan, this phase doc, and live Beads state before work.

## Closeout

S6 is accepted and closed. ChatGPT's exact discovery command now returns an authoritative match or empty list without comparing display names or emitting partial timeout/cap data. C2 owns the checkpoint review that follows.
