# Phase 00 Turn Doc: S0: Make the test baseline deterministic

Beads issue: `gh-forgejo-shim-c00`

Phase doc: `docs/implementation/chatgpt-forgejo-pr-compatibility/00-s0-make-the-test-baseline-deterministic.md`

## Accepted Outcome

Contributors get the same test result whether or not the host already has `/usr/bin/gh`.

## Orchestration Brief

```json
{
  "phase_issue_id": "gh-forgejo-shim-c00",
  "risk": "low",
  "execution_profile": "adaptive",
  "strategy": "direct",
  "implementation_owner": "Codex coordinator 01a02c9d-17aa-7ba0-b133-2e3913cd322c in the saved integration checkout",
  "review_independence": "A fresh read-only thermonuclear reviewer will inspect the accepted S0 diff after green.",
  "delegation_plan": [],
  "model_and_effort_rationale": "The implementation surface is two test files and one process boundary. Direct work keeps one mutable owner and makes the red and green commits easy to audit. No implementation helper can inspect an independent subsystem without duplicating this narrow work.",
  "required_evidence": [
    "A named regression fails because the fixture inherits the host PATH.",
    "The accepted green change makes the fixture PATH self-contained and keeps Git available through an explicit fixture.",
    "The narrow test, formatting, Clippy, the full workspace test suite, independent review, and exact-commit CI resolve cleanly."
  ],
  "user_constraints": [
    "Preserve accepted phase boundaries and the shared three-pass repair limit.",
    "Commit and push accepted red and green states separately.",
    "Do not reuse stale runner tasks."
  ]
}
```

CI owner: `phase coordinator`.

## Adaptations

None yet.

## Discoveries And Decisions

None yet.

## Implementation And Delegation Evidence

Red test added at the accepted CLI process seam:

- Renamed the affected doctor case to `doctor_ignores_an_installed_system_gh`.
- Removed its hand-built system `PATH`, so it now exercises the shared fixture contract.
- Removed the second affected bootstrap case's system-only `PATH` override.
- Ran `cargo test --workspace --locked --test cli_scaffold doctor_ignores_an_installed_system_gh -- --exact --nocapture`.
- Result: failed as intended. The process found `/usr/bin/gh` and printed `[ok] current PATH: bare gh resolves to /usr/bin/gh`.

No implementation helper was used. The accepted surface is one small shared fixture plus two known failing cases, so another implementation context would duplicate the same inspection while sharing a mutable checkout.

## Changed Behavior And Files

None yet.

## Review

Not started.

## CI And Gates

Owner: `phase coordinator`

State: `not-started`

Evidence:

Red gate: expected failure captured for the named isolation regression.

## PR And Commits

None yet.

## Beads Updates And Follow-Ups

Loop creation only.

## Plan Amendments

The user selected the adaptive profile when creating the loop. This supersedes the plan's earlier orchestrator-callback profile clause without changing product intent or phase boundaries.

## Context To Keep

Read the accepted plan, this phase doc, and live Beads state before work.

## Closeout

Not started.
